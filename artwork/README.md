# MakeStem app icon

Save the final square, 1024 x 1024 (or larger) PNG here as
`MakeStemIcon.png`, then run:

```sh
./scripts/build-app-icon.sh
```

The generated Xcode icon sizes are intentionally not tracked by Git.
